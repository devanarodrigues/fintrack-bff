---

# Artefato 3: `tasks.md` (Atualizado)

```markdown
# TASKS-FULL - Lista de Tarefas de Desenvolvimento

## FASE 1: Setup da Infraestrutura e Base
- [ ] **TASK-01**: Configurar workspace Angular 17+ com Standalone Components, TailwindCSS e suporte a Angular Material.
- [ ] **TASK-02**: Criar projeto Python FastAPI com Pydantic v2, SQLAlchemy, Migrações Alembic e suporte a CORS.
- [ ] **TASK-03**: Criar/Atualizar as tabelas no Banco de Dados (`Gasto` com colunas `valor_parcela`, `parcela_atual`, `total_parcelas` e `gasto_pai_id`).
- [ ] **TASK-04**: Implementar no Angular o `NavigationDrawerComponent` (Menu Hambúrguer) e o Header responsivo.

## FASE 2: PAGE-01 - Home Dashboard (FE-001)
- [ ] **TASK-05 [Python]**: Criar o endpoint `GET /api/v1/dashboard/summary` com cálculo das parcelas ativas com base em `valor_parcela`, `parcela_atual` e `total_parcelas`.
- [ ] **TASK-06 [Angular]**: Criar o `DashboardService` para consumo da API de resumo.
- [ ] **TASK-07 [Angular]**: Criar os componentes `MetricCard` e `ForecastCard`.
- [ ] **TASK-08 [Angular]**: Montar a `DashboardPageComponent` e integrar com o seletor de Mês/Ano.

## FASE 3: PAGE-02 - Gerenciar Gastos & OCR de Parcelas (FE-002, FE-003, FE-004)
- [ ] **TASK-09 [Python] - PARSER DE PARCELAS**: Implementar Regex no parser OCR para capturar a descrição, o valor da parcela individual, a `parcela_atual` e o `total_parcelas` (ex: "Notebook 03/10").
- [ ] **TASK-10 [Python] - SCHEMAS PYDANTIC**: Atualizar os DTOs de resposta para garantir o envio explícito dos campos `valor_parcela`, `parcela_atual` e `total_parcelas` para o frontend.
- [ ] **TASK-11 [Python] - DEDUPLICAÇÃO**: Ajustar o `ParcelMatchingService` para vincular o lançamento (ex: "Notebook 3/10") ao `gasto_pai_id` do histórico de parcelas anteriores (1/10 e 2/10), evitando duplicação.
- [ ] **TASK-12 [Angular] - MODAL DE REVISÃO (FE-004)**: Montar a interface do modal exibindo explicitamente: **Descrição**, **Valor da Parcela**, **Parcela Atual** e **Total de Parcelas** (ex: Notebook | R$ 250,00 | 3 / 10)[cite: 1].

## FASE 4: PAGE-03 - Visualizar Planilha (FE-005, FE-006)
- [ ] **TASK-13 [Python]**: Garantir que o endpoint `GET /api/v1/expenses` retorne a lista formatada com `valor_parcela`, `parcela_atual` e `total_parcelas`.
- [ ] **TASK-14 [Angular]**: Adicionar a coluna **Parcela** na Tabela Interativa exibindo a formatação `${parcelaAtual}/${totalParcelas}` (ex: `3/10`)[cite: 7, 8].
- [ ] **TASK-15 [Angular]**: Implementar alternância de abas entre "Visão Total" e "Visão por Cartão" com suporte a ordenação por valor da parcela e número da parcela[cite: 7, 8].

## FASE 5: PAGE-04 - Gerar Relatórios (FE-007)
- [ ] **TASK-16 [Python] - FORMATAÇÃO NO PDF**: Configurar a geração do relatório em PDF (ReportLab) para explicitar na tabela do documento o nome do item, o valor da parcela e o indicador da parcela (ex: "Notebook | R$ 250,00 | Parcela 3/10")[cite: 6].
- [ ] **TASK-17 [Angular]**: Criar a tela de parametrização de relatórios e acionar a exportação em PDF[cite: 6].

## FASE 6: PAGE-05 - Linha do Tempo (FE-008)
- [ ] **TASK-18 [Python]**: Criar endpoint `GET /api/v1/analytics/timeline` que projeta as parcelas futuras (`parcela_atual` até `total_parcelas`) nos meses subsequentes.
- [ ] **TASK-19 [Angular]**: Plotar o gráfico temporal (Chart.js) agrupando as despesas parceladas mês a mês.

## FASE 7: Testes Integrados
- [ ] **TASK-20**: Testar a extração e o retorno JSON de um item parcelado no OCR e validar se os atributos `valorParcela: 250.0`, `parcelaAtual: 3` e `totalParcelas: 10` chegam corretamente ao estado do Angular.