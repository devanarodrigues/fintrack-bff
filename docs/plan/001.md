---

# Artefato 2: `plan.md` (Atualizado com Mapeamento do Pydantic & Angular)

```markdown
# PLAN-FULL - Plano de Arquitetura e Implementação (Angular + Python BFF)

## 1. Mapeamento dos Schemas no Backend (Python FastAPI)

No Python, os Schemas Pydantic devem garantir a validação e exposição dos campos `valorParcela`, `parcelaAtual` e `totalParcelas`.

```python
# app/schemas/expense.py
from pydantic import BaseModel
from typing import Optional
from decimal import Decimal

class ExpenseResponse(BaseModel):
    id: str
    gasto_pai_id: Optional[str] = None
    data: str
    cartao_nome: str
    categoria: str
    descricao: str
    valor_parcela: Decimal
    tipo: str  # 'normal', 'parcelado', 'fixo'
    parcela_atual: int
    total_parcelas: int
    origem: str

    class Config:
        from_attributes = True